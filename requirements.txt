# Shinol AI Services - Requirements
# Install with: pip install -r requirements.txt

# Core
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0

# HTTP
httpx>=0.24.0
requests>=2.31.0

# TTS — edge-tts 7.2.7+ fixes 403 / WSS handshake on speech.platform.bing.com (Sec-MS-GEC, etc.)
# See: https://github.com/rany2/edge-tts/issues/458 — older 7.0.x often fails preview + /tts/generate
# If "No audio was received" regresses: https://github.com/rany2/edge-tts/issues/440
edge-tts>=7.2.7,<8

# Audio Processing
numpy>=1.24.0
scipy>=1.11.0
soundfile>=0.12.0

# AI Models
torch>=2.0.0
diffusers>=0.21.0
transformers>=4.30.0
accelerate>=0.20.0
faster-whisper>=0.10.0
kokoro>=0.9.0
# Japanese Kokoro voices (jf_* / jm_*): pulls Misaki ja + pyopenjtalk + unidic (fixes No module named 'pyopenjtalk')
# Same venv as: python -m uvicorn …   →   pip install "misaki[ja]"
# If UniDic data missing: python -m unidic download
misaki[ja]>=0.9.0,<0.10.0

# MeloTTS — Japanese local TTS. PyPI `melotts` often fails on Windows; use GitHub:
#   pip install git+https://github.com/myshell-ai/MeloTTS.git
# Required once per Python env (same venv as uvicorn): downloads ~500MB UniDic / fixes missing mecabrc
#   python -m unidic download
git+https://github.com/myshell-ai/MeloTTS.git
unidic>=1.1.0

# Image Processing
pillow>=10.0.0
opencv-python>=4.8.0

# Utilities
python-multipart>=0.0.6
